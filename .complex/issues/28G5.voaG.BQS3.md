Update `app/[locale]/court/page.js`:
- Replace translatable imports with `useCourt()` and `useAstrology()` hooks
- Keep non-translatable imports (symbols, element keys, suit/rank orders)
- Replace inline strings (headers, instructions, elemental combination text) with `useT()` lookups
- Prefix any cross-page links with `/${locale}/`