Extract all inline English strings from page components and Navigation into `i18n/ui/en.json`.

## Source files to scan
- `app/components/Navigation.js` — nav labels ('Decan', 'Court', 'Signs', 'Planets'), 'Coming soon' tooltip
- `app/page.js` — page title, subtitle, section headers ('Names', 'Card Meaning', 'Jungian Interpretation', developmental level labels), footer text
- `app/court/page.js` — headers, instructions, elemental combination text, rank/suit labels
- `app/signs/page.js` — headers, instructions, table headers
- `app/planets/page.js` — headers, table labels, section names
- `app/components/DeckSwitcher.js` — tooltip text ('Rider-Waite-Smith deck', 'Thoth deck')
- `app/layout.js` — metadata title and description

## Output structure (`i18n/ui/en.json`)
```json
{
  "nav": { "decan": "Decan", "court": "Court", ... },
  "decanPage": { "title": "...", "subtitle": "...", ... },
  "courtPage": { ... },
  "signsPage": { ... },
  "planetsPage": { ... },
  "deckSwitcher": { ... },
  "metadata": { "title": "...", "description": "..." }
}
```

## Notes
- Read each source file carefully to capture every user-visible string
- Use descriptive, nested keys (e.g., `decanPage.suitElement` not `label5`)
- Do NOT modify the source files yet — just create the en.json extraction