Update `app/[locale]/planets/page.js`:
- Replace translatable imports with `usePlanets()` and `useAstrology()` hooks
- Keep non-translatable imports (symbols, Chaldean order)
- Replace inline strings (table headers, section labels) with `useT()` lookups
- Prefix any cross-page links with `/${locale}/`