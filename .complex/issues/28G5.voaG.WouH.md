Update `app/[locale]/page.js` (Decan page — the largest page).

## Import changes
- Keep: `zodiacSymbols`, `zodiacElements` from `utils/astrology` (non-translatable)
- Keep: `decans` from `utils/decans` (structural, dates — though card names need translation)
- Replace: `zodiacDescriptions`, `elements` → `useAstrology()`
- Replace: `cardTitles`, `suitDescriptions`, `numberMeanings`, `jungianMeanings`, `developmentalMeanings` → `useCards()`
- Add: `useT()` for inline strings, `useLocale()` for link prefixing

## String replacements
- All section headers, labels, and instructions → `t.decanPage.*`
- Cross-page links (`/signs#...`, `/planets#...`) → `/${locale}/signs#...`, `/${locale}/planets#...`
- Footer text and GitHub link text → `t.common.*`