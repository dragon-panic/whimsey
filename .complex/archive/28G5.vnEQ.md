Translate `i18n/ui/en.json` → `i18n/ui/es.json`.

This covers all UI chrome: navigation labels, page titles, section headers, table headers, instructions, footer text, deck switcher tooltips, metadata.

Depends on OtRC (en.json must exist first). Copy the structure exactly, translate all values to Spanish.