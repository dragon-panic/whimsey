Create Spanish content translation files in `i18n/content/es/`.

Each file mirrors the translatable exports from its corresponding `utils/` file. Only string content is translated — structural data (symbols, dates, element keys, orders) stays in `utils/`.

See subtasks for per-file details. All 5 subtasks are parallel.