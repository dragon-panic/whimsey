Update `app/components/Navigation.js`:
- Import `useLocale()` and `useT()`
- Prefix all nav hrefs with `/${locale}` (e.g., `href={\"/${locale}/court\"}`)
- Replace hardcoded labels ('Decan','Court','Signs','Planets') with `t.nav.*`
- Update `isActive()` to account for locale prefix in pathname
- Add `<LanguageSwitcher />` next to `<DeckSwitcher />` in the nav bar