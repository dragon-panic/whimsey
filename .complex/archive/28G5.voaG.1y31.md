Update `app/[locale]/signs/page.js`:
- Replace translatable imports with `useSigns()`, `useAstrology()`, `usePlanets()` hooks
- Keep non-translatable imports (symbols, element keys)
- Replace inline strings with `useT()` lookups
- Prefix any cross-page links with `/${locale}/`